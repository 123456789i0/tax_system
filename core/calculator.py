# Requirement:
# - มี calc_tax(net_income) คำนวณภาษีตามขั้นบันไดจาก config
# - มี calc_tax_detail(net_income) คืนรายละเอียดภาษีแยกตามขั้น
# - มี calc_net_income(profile) คำนวณเงินได้สุทธิหลังหักค่าลดหย่อน
# - เป็น pure function คืนค่าเป็น float/list และห้ามใช้ input, print หรือไฟล์
