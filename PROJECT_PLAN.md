# แผนการทำงานโครงการระบบคำนวณภาษี

## 1. ภาพรวมโครงการ

พัฒนาโปรแกรมคอนโซลด้วย Python สำหรับคำนวณภาษีเงินได้บุคคลธรรมดา จัดการข้อมูลผู้เสียภาษี และแสดงผลสรุปภาษี โดยแบ่งระบบเป็นส่วนคำนวณข้อมูล ส่วนติดต่อผู้ใช้ และฟีเจอร์เสริม

เอกสารนี้กำหนดแนวทางการทำงานและกฎกลางของโครงการในระดับภาพรวม ยังไม่ลงรายละเอียดว่าฟังก์ชันใดต้องอยู่ในไฟล์ใด

## 2. วัตถุประสงค์

- คำนวณภาษีเงินได้ตามอัตราภาษีแบบขั้นบันได
- รองรับรายได้และค่าลดหย่อนหลายประเภท
- ตรวจสอบข้อมูลก่อนคำนวณและก่อนบันทึก
- บันทึก ค้นหา แก้ไข และลบข้อมูลผู้เสียภาษี
- แสดงผลการคำนวณให้เข้าใจง่าย
- ฝึกการออกแบบโปรแกรมแบบแบ่งส่วนและการทำงานร่วมกันเป็นทีม

## 3. ขอบเขตงาน

### งานหลัก

- รับข้อมูลผู้เสียภาษี รายได้ และค่าลดหย่อน
- คำนวณเงินได้สุทธิและภาษี
- จัดการข้อมูลแบบถาวร
- แสดงสรุปผล ตาราง และกราฟข้อความ
- ตรวจสอบเลขบัตรประชาชนและข้อมูลที่จำเป็น

### งานเสริม

- ควิซความรู้เกี่ยวกับภาษี
- การเปรียบเทียบแผนค่าลดหย่อน
- การส่งออกสรุปผลเป็นไฟล์ข้อความ

### งานที่ไม่รวมในโครงการ

- การยื่นภาษีจริงกับกรมสรรพากร
- การเชื่อมต่อธนาคารหรือบริการออนไลน์
- ระบบสมาชิกและสิทธิ์ผู้ใช้งานหลายระดับ
- การรับรองผลคำนวณเพื่อใช้แทนคำแนะนำทางภาษี

## 4. กฎกลางของข้อมูล

ข้อมูลผู้เสียภาษีให้ใช้โครงสร้างกลางร่วมกันทั้งระบบ ตัวอย่างเช่น:

```python
profile = {
    "id_card": "1234567890123",
    "name": "สมชาย ใจดี",
    "age": 35,
    "status": "single",
    "incomes": [
        {"type": "เงินเดือน", "amount": 480000.0},
        {"type": "ค่าเช่า", "amount": 60000.0},
    ],
    "deductions": {
        "personal": 60000.0,
        "spouse": 0.0,
        "insurance": 25000.0,
        "fund": 50000.0,
    },
}
```

กฎที่ต้องยึดร่วมกัน:

- `id_card` เป็นข้อความ 13 หลัก ไม่ควรเก็บเป็นตัวเลข
- `age` เป็นจำนวนเต็มที่ไม่ติดลบ
- `status` ใช้ค่าที่กำหนด เช่น `single` หรือ `married`
- `incomes` เป็นรายการข้อมูล โดยแต่ละรายการมีประเภทและจำนวนเงิน
- `deductions` เป็นข้อมูลค่าลดหย่อนแบบชื่อประเภทและจำนวนเงินหรือจำนวนหน่วย
- จำนวนเงินใช้ `float` ในการคำนวณ และปัดเศษเฉพาะตอนแสดงผล
- ข้อมูลที่ไม่ผ่านการตรวจสอบห้ามนำไปคำนวณหรือบันทึก

## 5. กฎกลางของฟังก์ชัน

ทุกส่วนต้องตกลงรูปแบบฟังก์ชันก่อนเริ่มเชื่อมระบบ โดยใช้ type hint และรับค่า/คืนค่าตามสัญญา เช่น:

```python
def calc_tax(net_income: float) -> float:
    ...


def calc_deduction(profile: dict) -> float:
    ...


def save_taxpayer(profile: dict) -> bool:
    ...


def find_taxpayer(id_card: str) -> dict | None:
    ...
```

กฎที่ต้องยึดร่วมกัน:

- ชื่อฟังก์ชันและพารามิเตอร์ใช้ภาษาอังกฤษแบบ `snake_case`
- ฟังก์ชันคำนวณต้องรับข้อมูลเข้าและคืนผลลัพธ์ ไม่รับ input หรือแสดงผลเอง
- ฟังก์ชันด้านหน้าจอรับผิดชอบการรับ input และการแสดงข้อความ
- ฟังก์ชันด้านข้อมูลรับผิดชอบการอ่านและเขียนไฟล์
- จำนวนเงินที่คืนจากชั้นคำนวณต้องยังไม่ถูกปัดเศษ
- เมื่อฟังก์ชันยังพัฒนาไม่เสร็จ ให้มี stub ที่คืนชนิดข้อมูลถูกต้องเพื่อให้ส่วนอื่นเรียกใช้งานได้
- การเปลี่ยนชื่อฟังก์ชัน รูปแบบข้อมูล หรือชนิดค่าที่คืน ต้องแจ้งทีมและแก้ข้อตกลงกลางก่อน

## 6. แผนการดำเนินงาน

### ระยะที่ 1: วิเคราะห์และออกแบบ

- สรุปปัญหา วัตถุประสงค์ และขอบเขต
- ตกลงโครงสร้าง `profile`
- ตกลงชื่อฟังก์ชัน ชนิดข้อมูล และค่าที่คืน
- กำหนดกฎภาษีและค่าลดหย่อนที่ใช้ในโครงการ

**ผลลัพธ์:** ข้อตกลงกลางที่ทุกคนใช้ร่วมกัน

### ระยะที่ 2: พัฒนาการคำนวณและตรวจสอบ

- พัฒนาการคำนวณเงินได้สุทธิ
- พัฒนาการคำนวณค่าลดหย่อน
- พัฒนาการคำนวณภาษีขั้นบันได
- พัฒนาการตรวจสอบข้อมูล
- ตรวจสอบด้วยกรณีตัวอย่างและค่าขอบเขต

**ผลลัพธ์:** ระบบคำนวณที่คืนผลลัพธ์ได้ถูกชนิดและตรวจสอบได้

### ระยะที่ 3: พัฒนาการจัดการข้อมูล

- วางรูปแบบการจัดเก็บข้อมูลถาวร
- เพิ่มการบันทึก โหลด ค้นหา แก้ไข และลบข้อมูล
- เพิ่มการส่งออกสรุปผล
- ทดสอบกรณีไฟล์ว่าง ไฟล์หาย และข้อมูลผิดรูปแบบ

**ผลลัพธ์:** ข้อมูลยังคงอยู่และเรียกกลับมาใช้งานได้

### ระยะที่ 4: เชื่อมส่วนติดต่อผู้ใช้

- ออกแบบเมนูหลักและลำดับการใช้งาน
- รับข้อมูลจากผู้ใช้และตรวจสอบข้อผิดพลาด
- แสดงผลการคำนวณในรูปแบบตารางและสรุป
- เชื่อมการทำงานของระบบหลักเข้าด้วยกัน

**ผลลัพธ์:** ผู้ใช้สามารถทำงานตั้งแต่กรอกข้อมูลจนถึงดูผลลัพธ์ได้

### ระยะที่ 5: ฟีเจอร์เสริม ทดสอบ และส่งมอบ

- เพิ่มควิซหรือฟีเจอร์เสริมตามเวลาที่เหลือ
- ทดสอบการทำงานรายส่วนและการทำงานร่วมกัน
- ทดสอบข้อมูลปกติ ข้อมูลว่าง ข้อมูลผิด และค่าขอบเขต
- แก้ไขข้อผิดพลาดและปรับปรุงเอกสาร
- เตรียมตัวอย่างการใช้งานและสาธิตระบบ

**ผลลัพธ์:** ระบบรุ่นพร้อมใช้งานเพื่อการศึกษาและเอกสารประกอบครบถ้วน

## 7. การแบ่งหน้าที่ทีม

| บทบาท | ความรับผิดชอบหลัก |
|---|---|
| Core | กฎภาษี การคำนวณเงินได้สุทธิ และค่าลดหย่อน |
| Data | การตรวจสอบ การจัดเก็บ และการส่งออกข้อมูล |
| UI | เมนู การรับข้อมูล และการแสดงผล |
| Extras และ QA | ฟีเจอร์เสริม การทดสอบ และเอกสาร |

การแบ่งงานเป็นระดับหน้าที่ ไม่ได้บังคับรายละเอียดภายในไฟล์ สมาชิกต้องประสานกันเมื่อมีการเปลี่ยนแปลงข้อมูลหรือฟังก์ชันกลาง

## 8. กำหนดการโดยประมาณ

| ช่วงเวลา | เป้าหมาย |
|---|---|
| สัปดาห์ที่ 1 | วิเคราะห์ ออกแบบ และตกลงกฎกลาง |
| สัปดาห์ที่ 2 | พัฒนาระบบคำนวณและตรวจสอบข้อมูล |
| สัปดาห์ที่ 3 | พัฒนาการจัดเก็บข้อมูลและเชื่อมเมนู |
| สัปดาห์ที่ 4 | ทดสอบ แก้บั๊ก จัดทำเอกสาร และสาธิต |

## 9. เกณฑ์ความสำเร็จ

- โปรแกรมเริ่มทำงานผ่านเมนูหลักได้
- ข้อมูลใช้โครงสร้างเดียวกันทุกส่วน
- ฟังก์ชันสำคัญรับและคืนค่าตามข้อตกลง
- คำนวณภาษีและค่าลดหย่อนได้ตามกฎที่กำหนด
- ข้อมูลผิดได้รับการแจ้งเตือนและไม่ทำให้โปรแกรมหยุดทำงาน
- ข้อมูลที่บันทึกสามารถโหลดกลับมาใช้ได้
- ผ่านการทดสอบกรณีปกติและกรณีผิดพลาด
- เอกสารการติดตั้ง การใช้งาน และแผนงานสอดคล้องกับระบบ

## 10. ข้อจำกัด

โปรแกรมนี้จัดทำเพื่อการศึกษา อัตราภาษีและกฎค่าลดหย่อนเป็นข้อมูลอ้างอิงตามที่ทีมกำหนดในช่วงพัฒนา ไม่ควรใช้แทนการคำนวณภาษีจริงหรือคำแนะนำจากผู้เชี่ยวชาญ
